# seq!
# parallel!
# any! {
#   max!: int,
#   min!: int,
#   contains!: list[str],
#   not_contains!: list[str],
# }
# ... <- any!: {}
# ..N <- any!: {max!: N}
# n.. <- any!: {min!: n}
# "n.N" <- any!: {min!: n, max!: N} (we'll need the quotes otherwise yaml will parse it as float)

# ..  <- any!: {min!: 1, max!: 1} ???
# -> inline chain ???
# (a \| b) inline parallel ???

# ? Com valido que el fitxer que m'ha creat compili
# [x] Option 1: The agent should do everything and we evaluate span properties
# [x] Option 2: Enable custom validators

# timbal.evals.cli <- custom_validators path or path to a config file
# screenshots!: evals/custom_validators/screenshots.py::validate_screenshots
# def validate_screenshots(path, prompt):
#     pass

- name: ecommerce # <- Name of the eval
  description: ... # <- Description of the eval

  params: # <- kwargs
    messages:
      - ...
      - ...
      - ...

  seq!:
    - llm
    - 1..
    - any!:
        not_contains: [todo_write]
        max: 3
    - todo_write:
        input:
          tasks:
            prompt!: should contains three tasks that say ...
          state:
            eq!: in_progress
    - ...
