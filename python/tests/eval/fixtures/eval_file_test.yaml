tests:
  - name: file_reading_test
    description: Test that agent can read and process files
    turns:
      - input:
          text: "What is the answer?"
          files:
            - "./math_question.md"
        output:
          validators:
            contains: ["4"]
