- name: file_reading_test
  description: Test that agent can read and process files
  turns:
    - input:
        prompt:
          - "What is the answer?"
          - "@./math_question.md"
      output:
        content:
          validators:
            contains: ["4"]
