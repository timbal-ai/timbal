tests:
  - name: file_reading_test
    description: Test that agent can read and process files
    turns:
      - input:
          prompt:
            - "What is the answer?"
            - "./math_question.md"
        output:
          validators:
            contains: ["4"]
