tests:
  - name: math_test
    description: Test mathematical operations
    turns:
      - input:
          prompt: "What is 2 + 3?"
        output:
          validators:
            contains: ["5"]
        steps:
          validators:
            contains:
              - name: add
                input:
                  a: "2"
                  b: "3"
