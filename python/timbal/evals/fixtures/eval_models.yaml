- name: "Eval 1"
  description: "Description for Eval 1"

  tags: ["something", "good"]
  # skip
  timeout: 30000
  # retries

  # env:
  #   API_KEY: ""

  # WIP explicit vs implicit
  # Runnable paths are not marked specially, since properties are a closed set
  agent: # <- root path (i.e. entry point runnable path)
    input:
      prompt: what time is it in madrid

    # output (we could validate the output here if we wanted)

    # Validators are not in a list, since keys should be unique
    seq!:
      - llm
      - get_datetime:
          input:
            timezone:
              equals!: "Europe/Madrid"

  seq!:
    - hey
